# Roadmap — Phase 1 Execution Plan

**Status:** Engineering milestones M0–M11 built. M0–M8 are complete and verified; M9–M10 are code-complete with content/infra/ops work parked; M11 is an assessment ([launch-readiness.md](launch-readiness.md)), not a sign-off. This is the engineering execution plan for [Phase 1 of the PRD](Adventure_Discovery_PRD_v1.1.md#phased-roadmap); it does not restate or override product scope, which stays authoritative in the PRD.

**What "done" means here:** the *platform* is built and works end-to-end (discovery, search + the binding zero-result relaxation, provenance-backed content model, ingestion, editorial publish, auth + saves, AI-assisted drafting, expiring dynamic data), with 80 unit tests + 12 E2E green and a CI that runs them against a real database. What remains is **not application code** — it's real content, external accounts, and human/legal sign-off. See [launch-readiness.md](launch-readiness.md) for exactly what a human does next.

Written for a solo build (one person + Claude Code) — milestones are sequenced, not parallelized across a team.

## How to read this

- **Sequencing is walking-skeleton-first, not PRD-layer-order.** The PRD documents the data model before the UI; we build the reverse — a thin, real, end-to-end slice on fake data first (M0–M3), *then* the provenance-backed content pipeline and admin tooling (M4–M9), then hardening and gate review (M10–M11). This trades spec-order for the fastest path to a real, clickable product loop.
- **Every milestone still has to satisfy the PRD's binding constraints** for whatever it touches — walking-skeleton-first changes the *order* of work, not the *bar* each piece has to clear once it's built for real (e.g., M3 still owes the exact zero-result relaxation behavior; M8 still can't let AI invent facts).
- Milestones M4 onward largely replace or extend what M0–M3 built with throwaway/simplified pieces (e.g., the M1 seed script is not the ingestion pipeline — it's scaffolding to unblock the UI, and gets superseded once M5 exists).

## Milestones

| # | Milestone | Status | Goal | Exit criteria |
|---|---|---|---|---|
| M0 | Project scaffolding | **Done** | Next.js (App Router) + TypeScript + Tailwind CSS 4 + shadcn/ui initialized; lint/format/typecheck configured; CI skeleton (install → lint → typecheck → test → build on PR); local Postgres + PostGIS via Docker Compose; Prisma (v7, driver-adapter based) connected; `/docs/adr/` created with the required ADRs stubbed. | App boots locally with a placeholder page ✓; lint/typecheck/test/build all pass locally ✓; `SELECT postgis_version()` succeeds against the local DB via `prisma db execute` ✓; all required ADRs exist as files ✓ (see [ADR tracker](#adr-tracker)). CI workflow is written but unverified — nothing has been pushed yet. |
| M1 | Minimal schema + seed data | **Done** | Trimmed Prisma schema — `Destination`, `Trail`, `DestinationTrail`, `MediaAsset` with enum facets and PostGIS geography columns. No provenance model yet (M4). Seed script with 6 fabricated destinations + 7 trails, real geometry. | `prisma migrate deploy` + `npm run db:seed` run cleanly; verified live — spatial columns/GiST indexes present, coordinates and routes round-trip via PostGIS, seed is idempotent. |
| M2 | Walking-skeleton discovery UI | **Done** | Home → Explore (card grid + 5 facet filters) → Destination (hero, facts, MapLibre map, trail list, safety disclosure) → Trail, all Server Components against the seeded DB. Domain-boundary folder structure per ARCHITECTURE.md. | Full click-through works on seed data (verified by screenshots + E2E), mobile-first, no spinners, map degrades gracefully — per the PRD's [Design Principles](Adventure_Discovery_PRD_v1.1.md#design-principles). |
| M3 | Search + zero-result relaxation | **Done** | Postgres FTS (`websearch_to_tsquery` over name/summary/tags/activities) + `pg_trgm` `word_similarity` typo tolerance; facet queries; the PRD's mandated [constraint-relaxation flow](Adventure_Discovery_PRD_v1.1.md#zero-result-constraint-relaxation) (drop order budget → month → trip length → difficulty; never activity/keyword) with a transparency banner and removable chips. | The zero-result relaxation Playwright test passes (binding PRD requirement) — plus 4 other E2E specs and 9 unit tests over the drop-order logic. |
| M4 | Full canonical data model | **Done** | Added `Source`, `SourceRecord`, `FactAssertion`, `PermitRequirement`, `ContentRevision`, and `OutboxEvent` (outbox groundwork for M5/M6). Polymorphic subjects (`subjectType`+`subjectId`) per the PRD. Seed rebuilt to be provenance-backed. Fact-precedence + freshness/expiry resolver ([precedence.ts](../src/platform/content-revisions/precedence.ts)) drives permit display. | Every seed record traces to a source record + content revision (6 revisions, 24 fact assertions, 6 permits); permit info renders on destination pages with official land-manager links and a "last verified" date; 10 unit tests cover precedence/expiry; E2E covers the official permit link. |
| M5 | Ingestion pipeline — first source | **Done** (live fetch parked on NPS key) | Built the [fetch → validate → normalize → store → persist-draft](ARCHITECTURE.md#5-data-ingestion-pipeline) pipeline in `src/platform/ingestion/`: NPS adapter (Zod-validated, maps only source-confirmable facts — never invents editorial facets or permit rules), checksummed raw-capture storage, idempotent SourceRecord + draft ContentRevision upserts, single-flight per source, per-record dead-letter queue, and auditable IngestionRun rows. Ingested data lands as an `in_review` draft revision; publish (M6) materializes the Destination. | Proven end-to-end via `npm run ingest:nps -- --fixture`: 2 drafts + 1 dead-lettered malformed record, **idempotent across re-runs** (no duplicates), raw captures archived. Live NPS fetch needs `NPS_API_KEY` (see DEPENDENCIES) but the whole pipeline is exercised offline. 12 unit tests (normalize/checksum/slugify); CI smokes the pipeline against a real DB. |
| M6 | Editorial admin tooling | **Done** (real auth is M7) | Protected `/admin`: data-health dashboard, review queue (with publish-readiness detection), draft editor (fill the editorial facets ingestion can't provide), destination list with unpublish, and source registry viewer. The **publish workflow** materializes an approved draft → canonical `Destination` + FactAssertions + PermitRequirement + geometry, emits a durable outbox event, and drains it to revalidate pages — all transactional and idempotent. Interim password gate (M7 swaps in Google OAuth + `is_admin`; the middleware/gate pattern stays). | Verified end-to-end: an NPS draft was filled and published into a live destination (7 facts, 1 permit, geometry, outbox event) **without touching SQL**, idempotently; admin auth gate + sign-in covered by 3 E2E tests; publish-validation + outbox-routing by unit tests. |
| M7 | Auth + saves | **Done** (live Google gated on OAuth creds) | Auth.js v5 + Prisma adapter, **database sessions** ([ADR-0002](adr/0002-authjs-session-strategy.md)), Google provider. User/Account/Session/VerificationToken + `isAdmin` + `SavedDestination` models. Save/unsave with sign-in gating, `/saved` list, `/signin`, account **data export** (JSON, no secrets) and **deletion** (cascade). Header reflects auth state. A non-production `/api/dev-login` mints a real DB session for testing without live Google. | Full flow E2E-tested: sign in → save → `/saved` → unsave, plus the signed-out sign-in prompt and the secrets-free export (12 E2E total). Live Google sign-in needs `AUTH_GOOGLE_ID/SECRET` (DEPENDENCIES). |
| M8 | AI-assisted drafting | **Done** (live Gemini gated on key) | `AiDraftProvider` interface with a Gemini Flash impl (env-gated) and a deterministic mock fallback. `draftWithAi` builds a source-only packet (PII-guarded), calls the provider, schema-validates output, and attaches a summary/tag *suggestion* to the revision with `origin=ai_assisted` + prompt version + source-packet hash. Admin "Suggest with AI" button. Full write-up: [ADR-0007](adr/0007-ai-provider-and-data-handling.md). | Verified: an NPS draft's AI pass set `origin=ai_assisted`, recorded prompt version + packet hash, summarized *only* the source text, left factual fields untouched, and stayed `in_review` (never auto-published). 13 unit tests (PII guard, hash, schema, mock). |
| M9 | Content population | **Partial** — adapters built; corpus population parked | Built the **Recreation.gov (RIDB)** adapter (same fixture-tested destination-draft pattern as NPS — proves the pipeline generalizes to a second source) and the **Open-Meteo forecast pipeline**: expiring `ForecastSnapshot`s, a freshness-gated read that never returns stale data, a scheduled refresh runner, and a forecast card on destination pages with full provenance ("general area outlook, not an on-trail observation"). | Adapter + normalizer fixture-tested; forecast freshness gating verified (expired snapshots are dropped, not shown). **Populating the real 25–50 destinations is parked** — it needs live API keys *and* human editorial judgment per rubric (the content-quality launch gate is a human sign-off, not code). OSM/Overpass (trail geometry) and USGS (elevation) adapters are designed-not-built — see note below. |
| M10 | Observability + security hardening | **Partial** — code built; ops parked | Built: secure headers + CSP ([securityHeaders.ts](../src/shared/config/securityHeaders.ts)), a rate limiter, the consent-gated analytics **event dictionary** (never emits PII), and a single error-reporting entry point. See [docs/security.md](security.md) for the full built-vs-parked breakdown. | Headers verified served; rate limiter, event-dictionary consent gating, and PII-stripping unit-tested (11 new tests). **Parked** (needs external infra/human process): Sentry/uptime accounts + alert runbooks, Postgres RLS roles, backups + restore drill, GTM/GA4 wiring + consent banner, threat model + legal docs. |
| M11 | Launch gate review | **Assessed** — no gate signed off (correct) | Walked every gate in [docs/launch-readiness.md](launch-readiness.md) with an honest built-vs-blocking state. Built the SEO gate's code deliverables: `robots.ts`, `sitemap.ts` (published canonical pages only), `noindex` on non-content routes, JSON-LD with only real facts. | Every gate has a documented state and owner. **No gate is signed off** — that's the correct outcome: gates need real content, external accounts, and human/legal sign-off, not more code. The launch-readiness doc is the roadmap for a human from here. |

### M0 notes (what actually happened vs. the plan)

- **Supabase was not provisioned.** No Supabase account/credentials exist yet, and none were needed — M0–M4 run against local Postgres+PostGIS via `docker-compose.yml` (`postgis/postgis:17-3.4`). Point `DATABASE_URL` at a real Supabase project only when actually deploying; that's a deploy-time task, not part of this roadmap's milestones as written. Note for Apple Silicon dev machines: this image has no native arm64 build and runs under emulation — functional but slower; revisit if local DB performance becomes noticeable.
- **`npm audit` currently reports 3 high-severity CVEs** in Next.js's own bundled transitive deps (`postcss`, `sharp`) with no non-breaking fix available yet — tracked in [ADR-0001](adr/0001-framework-version-and-caching-model.md), checked non-blockingly in CI. Re-run `npm audit` on every Next.js patch bump.
- **Prisma generated as v7.9.0**, which changed more than expected: no built-in query engine binary for SQL — it now requires an explicit driver adapter (`@prisma/adapter-pg` + `pg`) passed to `PrismaClient`. Prisma's own CLI installed reference docs under `.agents/skills/` (symlinked from `.claude/skills/`) — worth reading before assuming any pre-2026 Prisma pattern still applies, especially for M1's `Unsupported(...)` PostGIS field types.
- **CI is written but not yet verified live** — this repo has no commits from these milestones yet, so `.github/workflows/ci.yml` has never actually run on GitHub. Confirm both jobs (`checks` and `e2e`) go green on the first real PR.

### M1–M3 notes (what actually happened vs. the plan)

- **PostGIS + Prisma friction, resolved.** Geography columns are `Unsupported(...)` and nullable at the DB level (Prisma's typed `.create()` can't populate them — rows are created, then geometry written via parameterized raw SQL). Prisma 7 *does* accept `@@index(..., type: Gist)` on `Unsupported` columns, which is load-bearing: without the schema declaration, every `migrate dev` tries to DROP the GiST indexes. Full write-up in [ADR-0003](adr/0003-postgis-spatial-model.md).
- **Use `prisma migrate deploy`, not `migrate dev`, for the extension migrations.** `migrate dev` interactively prompts to reconcile drift it can't represent; `deploy` just applies pending migrations non-interactively.
- **Static generation deferred to M6.** The content pages read the DB, so they're `force-dynamic` for now — static generation at build would need a database in CI. Proper ISR/static generation for published pages (the PRD's caching model) is wired at M6 with a CI database and the outbox→revalidate path. Noted at the top of `src/app/page.tsx`.
- **`postinstall: prisma generate` added.** The generated client is gitignored, so CI and fresh clones regenerate it on install; without this, typecheck/build fail with no client.
- **Search is inline FTS + `word_similarity`, no stored tsvector/index.** Correct and fast for a 25–50 row corpus; a stored/indexed FTS column (or Meilisearch) is gated on measured need per [ADR-0004](adr/0004-search-graduation-criteria.md). `word_similarity` (not `similarity`) is what makes typos like "zionn" → "Zion Narrows Basecamp" match.
- **Map uses a keyless demo tile source (local dev only).** MapLibre renders; the real tile provider is still [ADR-0005](adr/0005-map-tile-provider.md). The map is progressive enhancement — it degrades to a static fallback and never blocks reading a destination.

### M4–M5 notes

- **Drafts, not direct rows.** Ingested content (M5) lands as an `in_review` `ContentRevision` (JSON body) keyed by `draftKey`, plus a `SourceRecord` — not a `Destination`. The Destination is materialized only at publish (M6) from an approved revision. This keeps the published-row schema strict and matches the PRD's "persist source record + canonical draft → review → publish canonical revision".
- **Editorial facets are never invented by ingestion.** The NPS adapter maps only what NPS confirms (name, coordinates, official URL, launch activities). Difficulty, trip length, budget, best months, and the real permit type are left for a human reviewer; the draft permit is `unknown` with the official URL.
- **Raw captures** are written to a gitignored `.ingestion-raw/` locally (a stand-in for the restricted R2 bucket) with checksummed, immutable keys — swap in R2 by implementing the `RawStore` interface.
- **Fixture-driven testing.** `fetchRaw` is injectable, so the pipeline runs fully offline against a bundled NPS fixture (including a deliberately malformed record to exercise the dead-letter queue). Live fetch is the only part gated on `NPS_API_KEY`.

### Parked in M9 (needs external keys and/or human editorial work)

- **The real launch corpus (25–50 destinations).** The pipeline, admin tooling, and rubric-gated publish flow are all built and proven, but populating the actual corpus requires live source keys **and** genuine human editorial judgment per rubric (difficulty, best-months, budget). That's exactly the human-in-the-loop work the PRD mandates — it cannot be automated. This is the main thing standing between "the platform works" and "the platform is launchable."
- **OSM/Overpass (trail geometry) and USGS (elevation) adapters.** The `SourceAdapter` interface accommodates them, but both target *trails* (geometry/elevation), not destination drafts, so they need a trail-ingestion target and Overpass/elevation query design — a larger effort deferred until real trail content is being built. Not required to demonstrate the pipeline (NPS + Recreation.gov already do).

## Key constraints worth re-reading before starting a milestone

Easy to lose track of in code; each of these is a binding PRD rule, not a suggestion:

- **M3** — a hard "no results" state is a product failure. Relaxation must be visible, labeled, and reversible (removable chip), never a silent widened query.
- **M5/M9** — no source may be ingested, transformed, or displayed until it has a reviewed entry in the [source registry](Adventure_Discovery_PRD_v1.1.md#source-registry-and-permitted-use). Raw captures are preserved with checksums; partial writes cannot publish a record.
- **M6** — publish/unpublish is a single action that emits the outbox event and triggers index/cache rebuild — not a direct DB write from the admin UI.
- **M8** — AI cannot fill factual gaps under any circumstance. It drafts from a supplied packet and stops; a human reviewer accepts responsibility for the published result.
- **All content milestones** — dynamic data (weather, closures, permit inventory) are expiring, attributable snapshots or official outbound links, never permanent fields. A stale snapshot gets removed from the UI, not shown anyway.

## ADR tracker

The PRD requires these decisions to live in `/docs/adr/` rather than the PRD itself. Status as of M3:

| ADR | Forces a decision by | Status |
|---|---|---|
| Framework version + caching model | M0 | **Accepted** — [ADR-0001](adr/0001-framework-version-and-caching-model.md) |
| Auth.js session strategy (DB vs. JWT sessions) | M7 | **Accepted** — database sessions ([ADR-0002](adr/0002-authjs-session-strategy.md)) |
| PostGIS and spatial model | M1 | **Accepted** — [ADR-0003](adr/0003-postgis-spatial-model.md) |
| Search graduation criteria (when Postgres FTS → Meilisearch) | M3 | Partial — inline FTS + pg_trgm shipped; numeric graduation thresholds still open in [ADR-0004](adr/0004-search-graduation-criteria.md) |
| Map tile provider + OSM compliance | M2 | Open — demo tiles in use for local dev only; production provider undecided ([ADR-0005](adr/0005-map-tile-provider.md)) |
| Source licences and refresh contracts | M5 | Not started |
| AI provider and data handling | M8 | Partial — Gemini integration + guards built; commercial terms/cost cap/eval set owed ([ADR-0007](adr/0007-ai-provider-and-data-handling.md)) |
| Analytics consent model | M10 | Partial — event dictionary + consent gate built; GTM/GA4 wiring + banner parked ([ADR-0008](adr/0008-analytics-consent-model.md)) |
| Backups, RPO/RTO | M10 | Not started — needs infra + a restore drill ([ADR-0009](adr/0009-backups-rpo-rto.md)) |
| Commercial service cost controls | M10/M11 | Not started — costed operating plan owed before beta ([ADR-0010](adr/0010-commercial-service-cost-controls.md)) |

## Explicitly still open (not resolved by this roadmap)

- **Which specific launch regions** make up the 25–50 Phase 1 destinations — the PRD requires "a deliberately limited set of launch regions" but doesn't name them.
- **Final product name and domain** — "Travel Roamer" is a PRD candidate pending trademark/domain clearance.
- **Map tile provider selection** — needed earlier than "before beta" in practice, since M2's map view depends on it.
- **No target launch date exists in the PRD.** This roadmap sequences work, not calendar time — add dates here if/when they're set, rather than inferring them.

## Phase 2 and Phase 3

Out of scope for this roadmap by design. See the PRD's [Phased Roadmap](Adventure_Discovery_PRD_v1.1.md#phased-roadmap) section — community contributions, expanded regions/activities, Meilisearch/pgvector/Redis, Spanish localization, and monetization all have their own release gates and shouldn't be pulled forward just because they'd be convenient to build alongside Phase 1 work.
